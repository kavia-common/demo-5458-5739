# BackendAPIService

This service exposes the Device Management REST API using Flask + pymongo.

- Runs on port 3001
- Requires MongoDB configuration via env vars
- See `.env.example` for required variables
- API documentation at /docs

Quickstart:
1. Create and export env vars (or source from a .env manager)
2. pip install -r requirements.txt
3. python run.py
